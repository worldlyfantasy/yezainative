<view class="page-shell favorites-page">
  <text class="back-link" bindtap="goBack">← 返回我的</text>
  <text class="page-eyebrow">收藏盒</text>

  <view wx:if="{{loading}}" class="section-block paper-block">
    <text class="favorites-section__title">正在读取收藏</text>
    <text class="page-subtitle favorites-empty__text">稍等片刻，正在同步你在云端的收藏内容。</text>
  </view>

  <view wx:elif="{{!loggedIn}}" class="section-block paper-block">
    <text class="favorites-section__title">登录后查看收藏</text>
    <text class="page-subtitle favorites-empty__text">{{loginHint}}</text>
  </view>

  <view wx:elif="{{errorText}}" class="section-block paper-block page-feedback">
    <text class="page-feedback__title">加载失败</text>
    <text class="page-feedback__desc">{{errorText}}</text>
  </view>

  <block wx:elif="{{hasFavorites}}">
    <view wx:if="{{favoriteCreators.length}}" class="section-block">
      <view class="section-head favorites-section__head">
        <view>
          <text class="section-eyebrow">创作者</text>
        </view>
        <text class="favorites-section__meta">{{favoriteCreators.length}} 位</text>
      </view>
      <view class="card-stack">
        <creator-card
          wx:for="{{favoriteCreators}}"
          wx:key="id"
          creator="{{item}}"
          bind:cardtap="onCreatorTap"
        ></creator-card>
      </view>
    </view>

    <view wx:if="{{favoriteServices.length}}" class="section-block">
      <view class="section-head favorites-section__head">
        <view>
          <text class="section-eyebrow">行程</text>
        </view>
        <text class="favorites-section__meta">{{favoriteServices.length}} 段</text>
      </view>
      <view class="card-stack">
        <service-card
          wx:for="{{favoriteServices}}"
          wx:key="id"
          service="{{item}}"
          creator-name="{{item.creatorName}}"
          bind:cardtap="onServiceTap"
        ></service-card>
      </view>
    </view>

    <view wx:if="{{favoriteIdeas.length}}" class="section-block">
      <view class="section-head favorites-section__head">
        <view>
          <text class="section-eyebrow">故事</text>
        </view>
        <text class="favorites-section__meta">{{favoriteIdeas.length}} 篇</text>
      </view>
      <view class="favorites-idea-list">
        <view
          wx:for="{{favoriteIdeas}}"
          wx:key="slug"
          class="favorites-idea-card"
          data-slug="{{item.slug}}"
          data-source-type="{{item.sourceType}}"
          data-wechat-article-url="{{item.wechatArticleUrl}}"
          data-wechat-article-title="{{item.wechatArticleTitle}}"
          data-title="{{item.title}}"
          bindtap="onIdeaTap"
        >
          <text class="section-eyebrow">主题：{{item.theme}}</text>
          <text class="favorites-idea-card__title">{{item.title}}</text>
          <text class="favorites-idea-card__summary">{{item.summary}}</text>
          <text wx:if="{{item.authorName}}" class="favorites-idea-card__meta">作者：{{item.authorName}}</text>
        </view>
      </view>
    </view>
  </block>

  <view wx:else class="section-block paper-block">
    <text class="favorites-section__title">还没有收藏内容</text>
    <text class="page-subtitle favorites-empty__text">去人物、行程或故事详情页，点一下标题右侧的收藏图标，卡片就会出现在这里。</text>
  </view>
</view>

<app-tab-bar></app-tab-bar>
